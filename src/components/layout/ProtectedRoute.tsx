"use client"
import React from "react"

export default function ProtectedRoute({ children }: { children: React.ReactNode }) {
  // Placeholder: integrate with auth/session checks
  return <div>{children}</div>
}
